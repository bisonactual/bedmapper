# Native Vision Sidecar

The production `bedmapper-vision` sidecar is kept in a private repository and
checked out by CI at `src-native/bedmapper-vision`.

For local development, clone the private sidecar into this directory before
building:

```bash
git clone git@github.com:<owner>/bedmapper-vision.git src-native/bedmapper-vision
```
